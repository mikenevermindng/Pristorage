version: '3'

services:
  webapp:
    ports: 
      - 1234:1234
    build: 
      context: ./
      dockerfile: Dockerfile
    volumes:
      - ../../:/usr/src/app
      - /usr/src/app/node_modules
      - /usr/src/app/.next